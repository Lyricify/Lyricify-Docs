---
import LastUpdated from 'virtual:starlight/components/LastUpdated';
import Pagination from 'virtual:starlight/components/Pagination';
import { Icon } from '@astrojs/starlight/components';
import LanguagePreferenceScript from './LanguagePreferenceScript.astro';

const currentYear = new Date().getFullYear();
const locale = Astro.currentLocale ?? Astro.locals.starlightRoute?.lang ?? 'zh-CN';
const localeLower = locale.toLowerCase();
const isTraditionalChinese = localeLower.startsWith('zh-hant');
const copyrightText = localeLower.startsWith('en')
	? `Copyright © ${currentYear} WXRIW. All rights reserved.`
	: isTraditionalChinese
		? `版權所有 © ${currentYear} WXRIW，保留所有權利`
		: `版权所有 © ${currentYear} WXRIW，保留所有权利`;
const { editUrl, isFallback, entry } = Astro.locals.starlightRoute;
const isSplashPage = entry.data.template === 'splash';
const isAutoTranslatedPage = Boolean(entry.data.autoTranslated);
const showCustomEditLink = Boolean(editUrl && !isFallback && !isSplashPage && !isAutoTranslatedPage);
const editLinkText = localeLower.startsWith('en')
	? 'Edit this page on GitHub'
	: isTraditionalChinese
		? '在 GitHub 上編輯此頁'
		: '在 GitHub 上编辑此页';
---

<footer class="sl-flex">
	<div class="meta sl-flex">
		<LastUpdated />
	</div>

	{
		showCustomEditLink && (
			<div class="edit-link-wrap not-content">
				<a href={editUrl} class="edit-link-card">
					<Icon name="pencil" size="1rem" />
					<span>{editLinkText}</span>
				</a>
			</div>
		)
	}

	<Pagination />

	<div class="lyricify-footer not-content">
		<div class="brand">
			<img class="logo" src="/Lyricify-icon.png" alt="Lyricify Icon" />
			<div class="text">
				<p class="name">Lyricify</p>
				<p class="copyright">{copyrightText}</p>
			</div>
		</div>
	</div>

	<LanguagePreferenceScript />
</footer>

<style>
	@layer starlight.core {
		footer {
			flex-direction: column;
			gap: 1.5rem;
		}

		.meta {
			gap: 0.75rem 3rem;
			justify-content: space-between;
			flex-wrap: wrap;
			margin-top: 3rem;
			font-size: var(--sl-text-sm);
			color: var(--sl-color-gray-3);
		}

		.meta > :global(p:only-child) {
			margin-inline-start: auto;
		}
	}

	.lyricify-footer {
		margin-top: 1rem;
		padding-top: 1.25rem;
		border-top: 1px solid var(--sl-color-gray-6);
	}

	.edit-link-wrap {
		margin-top: 0.25rem;
	}

	.edit-link-card {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0;
		border: 0;
		border-radius: 0;
		background: transparent;
		color: var(--sl-color-gray-3);
		font-size: var(--sl-text-sm);
		font-weight: 400;
		line-height: 1.4;
		text-decoration: none;
	}

	.edit-link-card:hover {
		color: var(--sl-color-white);
		text-decoration: none;
	}

	.brand {
		display: flex;
		align-items: center;
		gap: 0.875rem;
	}

	.logo {
		width: 2.75rem;
		height: 2.75rem;
		flex: 0 0 auto;
	}

	.text {
		display: flex;
		flex-direction: column;
		gap: 0.125rem;
	}

	.name,
	.copyright {
		margin: 0;
	}

	.name {
		font-size: var(--sl-text-base);
		font-weight: 700;
		color: var(--sl-color-white);
	}

	.copyright {
		font-size: var(--sl-text-sm);
		color: var(--sl-color-gray-3);
	}

	@media (max-width: 40rem) {
		.brand {
			align-items: flex-start;
		}
	}
</style>
