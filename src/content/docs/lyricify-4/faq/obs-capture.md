---
title: 使用 OBS 捕获桌面歌词
---

因为这个功能的特殊性，所以没有在设置页面中提供修改选项，目前需要手动修改配置文件。

打开 `设置`，找到 `高级` `设置文件` `打开`，然后找到 `enable_obs_capture` 属性，将 `false` 修改为 `true` 后保存文件，然后点击 `加载` 即可。

注意，如果捕获后画面为全黑，可尝试切换捕获方式。另外，“锁定桌面歌词”也可能会影响 OBS 的正常捕获。如遇更多问题，建议咨询 OBS 社区。
